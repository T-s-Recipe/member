spring:
  application:
    name: member
  data:
    mongodb:
      uri: ${MONGO_URL}
    redis:
      repositories:
        enabled: false

server:
  port: 8020

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs