spring:
  webflux:
    base-path: /members
  application:
    name: member
  data:
    mongodb:
      uri: ${MONGO_URL}
    redis:
      repositories:
        enabled: false

server:
  port: 8020

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false